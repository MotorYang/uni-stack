server:
  port: 8080

spring:
  application:
    name: gateway
  # JDK 21 虚拟线程优化
  threads:
    virtual:
      enabled: true

  cloud:
    nacos:
      username: uni-stack
      password: 745700Yxy@
      discovery:
        server-addr: ${NACOS_SERVER:192.168.1.97:8848}
        namespace: ${NACOS_NAMESPACE:uni-stack}
      config:
        server-addr: ${NACOS_SERVER:192.168.1.97:8848}
        namespace: ${NACOS_NAMESPACE:uni-stack}
        file-extension: yaml

  config:
    import:
      - nacos:public-config.yaml?group=DEFAULT_GROUP&refreshEnabled=true
      - nacos:gateway.yaml?group=DEFAULT_GROUP&refreshEnabled=true